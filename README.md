# 🕌 بوت أوقات الصلاة الإسلامي

بوت تيليجرام متكامل لمتابعة أوقات الصلاة مع دعم متعدد المجموعات وإرسال إشعارات الأذان الذكية.

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![Python](https://img.shields.io/badge/python-3.8+-green)

---

## 📋 المميزات الرئيسية

✅ **دعم متعدد المجموعات**: دعم غير محدود من المجموعات، كل مجموعة بإعداداتها الخاصة
✅ **أوقات ديناميكية**: تتغير أوقات الصلاة فوراً عند تغيير المدينة
✅ **حفظ إجباري**: حفظ الأوقات في قاعدة البيانات فوراً
✅ **أذان ذكي**: خدمة أذان لكل مجموعة على حدة حسب توقيتها
✅ **منع التكرار**: نظام منع إرسال نفس الأذان مرتين
✅ **تسجيل الصلوات**: نظام نقاط ومتسابقين
✅ **محتوى ديني**: أذكار وأحاديث دورية
✅ **تحسينات حديثة**: منع الطلبات المتكررة للـ API، وإصلاح مشاكل Telegram API

---

## 🔄 التحديثات الأخيرة (v1.0.1)

### الإصلاحات المضافة:
- ✅ **إصلاح مشكلة اختيار نفس المدينة**: الآن عند اختيار نفس المدينة، يظهر تنبيه بدلاً من خطأ
- ✅ **تقليل الطلبات المتكررة**: تم إضافة cache لتقليل الطلبات المتكررة إلى الـ API
- ✅ **تحسين معالجة الأخطاء**: معالجة أفضل لـ Network Errors

---

## 📦 المتطلبات

- Python 3.8 أو أحدث
- Telegram Bot Token (من [@BotFather](https://t.me/BotFather))
- اتصال بالإنترنت لجلب الأوقات من API

---

## 🚀 التثبيت والإعداد

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd prayer-times-bot
```

### 2. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

أو:

```bash
pip install pyTelegramBotAPI requests schedule pytz python-dotenv
```

### 3. الحصول على توكن البوت

1. افتح [@BotFather](https://t.me/BotFather) في تيليجرام
2. أرسل `/newbot`
3. اتبع التعليمات للحصول على توكن البوت
4. انسخ التوكن الذي ستحصل عليه (مثال: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4. إعداد المتغيرات البيئية

أنشئ ملف `.env` في المجلد الرئيسي:

```env
BOT_TOKEN=your_bot_token_here
DATABASE_PATH=prayer_bot.db
CONTENT_FILE=content.json
```

أو عدل متغير `BOT_TOKEN` مباشرة في ملف `bot.py`:

```python
BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'  # استبدل هذا بـ توكن البوت الخاص بك
```

### 5. تشغيل البوت

```bash
python bot.py
```

أو:

```bash
python3 bot.py
```

---

## 📖 الأوامر المتاحة

| الأمر | الوصف |
|-------|-------|
| `/start` | بدء البوت والتسجيل في النظام |
| `/setup` | إعداد المجموعة (يجب تنفيذه في المجموعة) |
| `/setgroupcity` |(for admin only) تغيير مدينة المجموعة |
| `/groupstatus` | عرض إعدادات المجموعة وأوقات الصلاة |
| `/prayed` | تسجيل صلاة يدوياً |
| `/top` | عرض لوحة المتصدرين |
| `/rules` | عرض قوانين البوت |
| `/help` | عرض دليل المساعدة |
| `/contentstatus` | (for admin only)يعرض لك الجدول الزمني للأذكار (متى يتم إرسال أذكار الصباح، المساء، والأحاديث) وهل هي مفعلة أم لا. |
| `/sendcontent` | إرسال فوري لذكر أو حديث عشوائي إلى المجموعة (بدون انتظار الجدول الزمني). مفيد لتنشيط المجموعة. |
| `/status` | (for admin only) يعرض تقريراً تقنياً مفصلاً (عدد المستخدمين الكلي، حالة الاتصال بالـ API، حالة قاعدة البيانات، وعدد المجموعات النشطة). |
| `/reset_all` | (for admin only)(أمر خطير) يقوم بمسح كافة بيانات المجموعة (تصفير النقاط، حذف الإعدادات، حذف السجلات) ويعيد المجموعة كأنها جديدة. يطلب تأكيداً قبل التنفيذ. |
---

## ⚙️ كيفية الإعداد

### 1. إعداد البوت في المجموعة

1. أضف البوت إلى مجموعتك
2. اجعل البوت **مشرفاً** (مطلوب لعمل الأزرار التفاعلية)
3. أرسل الأمر `/setup`
4. البوت سيرسل تأكيد الإعداد

### 2. تحديد المدينة

1. أرسل الأمر `/setgroupcity`
2. اختر مدينتك من القائمة المتاحة
3. سيتم تحديث الإعدادات وجلب الأوقات فوراً
4. أرسل `/groupstatus` للتأكد من التحديث

### 3. تفعيل الإشعارات

الإشعارات تعمل تلقائياً بعد إعداد المجموعة وتحديد المدينة.

سيتم إرسال إشعار الأذان في الوقت الصحيح حسب توقيت مدينتك.

---

## 📊 قاعدة البيانات

يستخدم البوت قاعدة بيانات SQLite مع الجدول التالي:

### الجداول الرئيسية

| الجدول | الوصف |
|--------|-------|
| `users` | بيانات المستخدمين والنقاط |
| `groups` | إعدادات المجموعات |
| `prayer_times_per_group` | أوقات الصلاة لكل مجموعة |
| `azan_sent_log` | سجل إرسال الأذان (لمنع التكرار) |
| `prayer_logs` | سجل صلوات المستخدمين |
| `settings` | الإعدادات العامة |

---

## 🔧 ملفات المشروع

```
prayer-times-bot/
│
├── bot.py                  # الملف الرئيسي للبوت
├── database_schema.sql     # هيكل قاعدة البيانات
├── content.json           # المحتوى الديني (أذكار وأحاديث)
├── requirements.txt       # المكتبات المطلوبة
├── README.md              # ملف القراءة الرئيسي
├── CHANGES.md             # سجل التغييرات
├── .env.example           # نموذج للمتغيرات البيئية
├── .env                   # المتغيرات البيئية (أنشئه بنفسك)
├── prayer_bot.db          # قاعدة البيانات (تُنشأ تلقائياً)
└── bot.log                # سجل البوت (يُنشأ تلقائياً)
```

---

## ✅ قائمة التحقق النهائية

قبل تشغيل البوت، تأكد من الآتي:

### الإعداد والتهيئة

- [x] تثبيت جميع المكتبات المطلوبة من `requirements.txt`
- [x] الحصول على توكن البوت من [@BotFather](https://t.me/BotFather)
- [x] تعيين `BOT_TOKEN` في `.env` أو في `bot.py`
- [x] إنشاء ملف `.env` (اختياري) أو تعديل `BOT_TOKEN` مباشرة

### اختبار البوت

- [ ] عند استخدام `/setgroupcity` وتغيير المدينة، يظهر هذا التغيير فوراً في `/groupstatus`
- [ ] جدول `prayer_times_per_group` يحتوي على بيانات الأوقات المناسبة للمجموعة
- [ ] المجدول الديناميكي (`azan_scheduler_loop`) يعمل ويتحقق من الوقت لكل مجموعة على حدة
- [ ] عند وصول وقت الأذان، يتم إرسال الرسالة
- [ ] لا يتم إرسال نفس الأذان مرتين لنفس المجموعة في نفس اليوم (بفضل `azan_sent_log`)
- [ ] `/top` يعرض المستخدمين بناءً على عدد صلواتهم ونقاطهم
- [ ] المحتوى الديني (أذكار وأحاديث) يُرسل بشكل صحيح

### اختبار المجموعات المتعددة

- [ ] يمكن إضافة البوت لمجموعات متعددة
- [ ] كل مجموعة لها إعداداتها الخاصة (مدينة، توقيت)
- [ ] إشعارات الأذان تُرسل لكل مجموعة حسب توقيتها
- [ ] تغيير مدينة مجموعة لا يؤثر على مجموعات أخرى

---

## 🛡️ الأمان والأداء

- **استخدام SQLite**: قاعدة بيانات خفيفة وسريعة
- **معالجة الأخطاء**: `try-except` لجميع الدوال الرئيسية
- **تسجيل الأخطاء**: نظام `logging` متكامل
- **مجدول منفصل**: يعمل في خيط (thread) منفصل
- **منع التكرار**: نظام فريد لمنع إرسال نفس الإشعار

---

## 📝 التسجيل

البوت يسجل جميع الأحداث في ملف `bot.log`:

```
2024-01-15 12:00:00 - __main__ - INFO - Bot polling started
2024-01-15 12:00:05 - __main__ - INFO - User 12345 started the bot
2024-01-15 12:00:10 - __main__ - INFO - Group 67890 updated to city Cairo, Egypt
```

---

## 🤝 المساهمة

المساهمات مرحب بها! يرجى:

1. Fork المشروع
2. إنشاء فرع جديد للميزة
3. Commit التغييرات
4. Push للفرع
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

## 🐛 المشاكل الشائعة

### المشكلة: البوت لا يستجيب للأوامر

**الحل:**
1. تأكد من أن البوت مشرف في المجموعة
2. تأكد من صحة توكن البوت
3. تأكد من أن البوت يعمل (تحقق من `bot.log`)

### المشكلة: لا يتم إرسال إشعارات الأذان

**الحل:**
1. تأكد من إعداد المجموعة (`/setup`)
2. تأكد من تحديد المدينة (`/setgroupcity`)
3. تأكد من تحميل الأوقات (`/groupstatus`)

### المشكلة: التوقيت غير صحيح

**الحل:**
1. تأكد من اختيار المدينة الصحيحة
2. تأكد من أن التوقيت في قاعدة البيانات صحيح
3. استخدم `/setgroupcity` لتغيير المدينة

---

## 📞 الدعم

للدعم والاستفسارات، تواصل مع:

- [@BotFather](https://t.me/BotFather) - للحصول على التوكن وإدارة البوت
- [@TelegramApi](https://t.me/TelegramApi) - وثائق API تيليجرام

---

## 📚 المراجع

- [pyTelegramBotAPI Documentation](https://github.com/eternnoir/pyTelegramBotAPI)
- [Aladhan API Documentation](https://aladhan.com/prayer-times-api)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

## 🌟 المؤلف

تم تطوير هذا البوت بمجهود شخصي مع مساعده الذكاء الاصطناعي في تنظيم الكود
---

## 🙏 شكر وتقدير

- شكراً لـ [Aladhan API](https://aladhan.com/) على خدمة أوقات الصلاة المجانية
- شكراً لفريق تيليجرام على API الرائع
- شكراً للمسلمين على استخدام هذا البوت

---

**نسأل الله أن ينفع بنا وبكم ويتقبل منا ومنكم صالح الأعمال 🤲**

---

*آخر تحديث: يناير 2025*
